// Placeholder — real routes/screens arrive in Task 5. This just proves the
// Vite + Tailwind + design-system pipeline is wired end to end.
export function App() {
  return (
    <div className="flex min-h-screen items-center justify-center bg-brand-cream px-4">
      <div className="rounded-lg bg-surface p-6 text-center shadow">
        <h1 className="text-xl font-semibold text-brand-brown">Mason Connect</h1>
        <p className="mt-2 text-sm text-brand-ebony">App scaffold pronto.</p>
      </div>
    </div>
  );
}

export default App;
